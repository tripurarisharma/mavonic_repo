// Template Literals
// Template literals are enclosed in backtick (`) characters instead of double quotes (") or single quotes ('). This allows you to define multi-line strings and embed expressions within the string itself.

let word1='Suraj';
let word2='Sharma';

const fullName=`${word1} ${word2}`;

console.log(fullName)


// Output : Suraj Sharma