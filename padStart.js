let example="suraj";
console.log(example.padStart(10,"a"));

//output:aaaaasuraj

// The padStart() method pads the current string with another string (multiple times) until the resulting string reaches the given length. 